import { pallete } from "../../../util/styles/pallete";

export const styles = {
  button: {
    backgroundColor: pallete.colors.primaryColor,

    "&:hover": {
      backgroundColor: pallete.colors.primaryColor,
    },
  },
};
