import { pallete } from "../../../util/styles/pallete";

export const styles = {
  button: {
    backgroundColor: pallete.colors.editColor,

    "&:hover": {
      backgroundColor: pallete.colors.editColor,
    },
  },
};
