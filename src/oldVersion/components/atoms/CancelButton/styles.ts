import { pallete } from "../../../util/styles/pallete";

export const styles = {
  button: {
    backgroundColor: pallete.colors.cancelColor,

    "&:hover": {
      backgroundColor: pallete.colors.cancelColor,
    },
  },
};
